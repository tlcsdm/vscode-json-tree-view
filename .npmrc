# typescript-eslint@8.x peer-dep constraint is typescript<6.1.0, but this project
# uses typescript@7. Setting legacy-peer-deps=true allows npm to install without
# failing on the peer conflict; npm list --production still succeeds as needed by vsce.
legacy-peer-deps=true
